/**
 * Home page — AI Car Mechanic chat shell.
 *
 * Stage 1: Static shell only.
 * - Empty message list with placeholder empty state
 * - Text input + Send button
 * - Placeholder attach buttons for image / audio / video (non-functional)
 *
 * Stage 2 will wire up real API calls and streaming responses.
 */

import ChatShell from "@/components/ChatShell";

export default function HomePage() {
  return <ChatShell />;
}

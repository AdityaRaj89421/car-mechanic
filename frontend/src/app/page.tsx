/**
 * Home page — AI Car Mechanic chat shell.
 * Stage 4: fully wired — chat, uploads, diagnosis card, booking modal.
 */

import { ChatShell } from "@/components/ChatShell";

export default function HomePage() {
  return <ChatShell />;
}
